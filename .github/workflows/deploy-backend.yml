name: Deploy Backend to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID || vars.GCP_PROJECT_ID }}
  REGION: asia-northeast1
  SERVICE_NAME: universegeo-backend

jobs:
  deploy-backend:
    name: Deploy Backend to Cloud Run
    runs-on: ubuntu-latest
    
    environment:
      name: Environment secrets
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug Secrets
        run: |
          echo "🔍 デバッグ情報:"
          echo "GitHub Event: ${{ github.event_name }}"
          echo "GitHub Ref: ${{ github.ref }}"
          echo "GitHub Repository: ${{ github.repository }}"
          echo ""
          echo "📋 Secrets確認（値は表示しません）:"
          echo "GCP_SA_KEY exists: $([ -n "${{ secrets.GCP_SA_KEY }}" ] && echo 'YES' || echo 'NO')"
          echo "GCP_SA_KEY length: $(echo -n "${{ secrets.GCP_SA_KEY }}" | wc -c)"
          echo "GCP_PROJECT_ID exists: $([ -n "${{ secrets.GCP_PROJECT_ID }}" ] && echo 'YES' || echo 'NO')"
          echo "GCP_PROJECT_ID value: ${{ secrets.GCP_PROJECT_ID }}"
          echo ""
          echo "📋 環境変数:"
          echo "PROJECT_ID: ${{ env.PROJECT_ID }}"
          echo "REGION: ${{ env.REGION }}"
          echo "SERVICE_NAME: ${{ env.SERVICE_NAME }}"
          echo ""
          echo "📝 確認方法:"
          echo "1. https://github.com/${{ github.repository }}/settings/secrets/actions を開く"
          echo "2. Repository secrets タブで以下を確認:"
          echo "   - GCP_SA_KEY が存在するか"
          echo "   - GCP_PROJECT_ID が存在するか"
          echo "   - Secret名が正確に一致しているか（大文字・小文字）"

      - name: Verify Secrets
        run: |
          echo "🔍 Secret確認中..."
          echo ""
          
          # GCP_SA_KEY の確認（Repository secrets または Environment secrets）
          GCP_SA_KEY_VALUE='${{ secrets.GCP_SA_KEY }}'
          GCP_SA_KEY_LENGTH=$(echo -n "$GCP_SA_KEY_VALUE" | wc -c)
          
          echo "GCP_SA_KEY length: $GCP_SA_KEY_LENGTH"
          
          if [ -z "$GCP_SA_KEY_VALUE" ] || [ "$GCP_SA_KEY_LENGTH" -eq 0 ] || [ "$GCP_SA_KEY_VALUE" = "***" ]; then
            echo "❌ エラー: GCP_SA_KEY が設定されていません"
            echo ""
            echo "📝 設定方法（Environment secrets）:"
            echo ""
            echo "このワークフローは 'environment: Environment secrets' を設定しているため、"
            echo "Environment secrets を参照します。"
            echo ""
            echo "設定手順:"
            echo "1. https://github.com/${{ github.repository }}/settings/environments/Environment%20secrets を開く"
            echo "2. Environment secrets セクションで Add secret をクリック"
            echo "3. Name: GCP_SA_KEY（大文字・小文字を正確に）"
            echo "   ⚠️ 重要: Secret名は 'GCP_SA_KEY' と完全に一致する必要があります"
            echo "   ❌ 間違い: gcp_sa_key, GCP-SA-KEY, GCP_SA_KEY_ など"
            echo "4. Secret: サービスアカウントキー（JSON）の内容全体を貼り付け"
            echo "   - JSON全体（{ から } まで）をそのまま貼り付け"
            echo "   - 改行も含めてそのまま貼り付け"
            echo "5. Add secret をクリック"
            echo ""
            echo "⚠️ 確認事項:"
            echo "- Environment secrets に 'GCP_SA_KEY' が存在するか確認"
            echo "- Secret名が正確に 'GCP_SA_KEY' であるか確認（大文字・小文字）"
            echo "- JSONファイルの内容全体をコピー&ペーストしているか確認"
            exit 1
          fi
          
          echo "✅ GCP_SA_KEY が設定されています（長さ: $GCP_SA_KEY_LENGTH 文字）"
          
          # JSON形式の確認
          if echo "$GCP_SA_KEY_VALUE" | grep -q '"type": "service_account"'; then
            echo "✅ JSON形式が正しいです"
          else
            echo "⚠️ 警告: JSON形式が正しくない可能性があります"
          fi
          
          # GCP_PROJECT_ID の確認
          GCP_PROJECT_ID_VALUE="${{ secrets.GCP_PROJECT_ID }}"
          if [ -z "$GCP_PROJECT_ID_VALUE" ]; then
            echo "❌ エラー: GCP_PROJECT_ID が設定されていません"
            echo ""
            echo "📝 設定方法:"
            echo "1. Settings > Secrets and variables > Actions > Repository secrets"
            echo "2. Name: GCP_PROJECT_ID"
            echo "3. Secret: univere-geo-demo"
            exit 1
          fi
          echo "✅ GCP_PROJECT_ID: $GCP_PROJECT_ID_VALUE"
          
          # その他のSecretsの確認
          echo ""
          echo "📋 その他のSecrets確認:"
          echo "BQ_DATASET: $([ -z "${{ secrets.BQ_DATASET }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"
          echo "GOOGLE_SPREADSHEET_ID: $([ -z "${{ secrets.GOOGLE_SPREADSHEET_ID }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"
          echo "GOOGLE_SHEETS_API_KEY: $([ -z "${{ secrets.GOOGLE_SHEETS_API_KEY }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"
          echo "BACKEND_SERVICE_ACCOUNT: $([ -z "${{ secrets.BACKEND_SERVICE_ACCOUNT }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Verify Backend Files
        working-directory: ./backend
        run: |
          echo "🔍 バックエンドファイルの確認:"
          echo ""
          echo "📁 ディレクトリ構造:"
          ls -la
          echo ""
          echo "📄 重要なファイルの確認:"
          echo "  Dockerfile: $([ -f Dockerfile ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo "  package.json: $([ -f package.json ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo "  tsconfig.json: $([ -f tsconfig.json ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo "  src/index.ts: $([ -f src/index.ts ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo ""
          if [ -f Dockerfile ]; then
            echo "📋 Dockerfileの内容:"
            head -20 Dockerfile
          fi

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          source: ./backend
          region: ${{ env.REGION }}
          project_id: ${{ env.PROJECT_ID }}
          flags: '--allow-unauthenticated --memory 512Mi --cpu 1 --max-instances 10'
        id: deploy

      - name: Set Environment Variables to Cloud Run
        if: always()
        run: |
          echo "🔧 GitHub Secretsから環境変数をCloud Runに設定中..."
          echo ""
          
          # リージョンの値を明示的に設定
          REGION_VALUE="${{ env.REGION }}"
          if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
            REGION_VALUE="asia-northeast1"
          fi
          
          # 環境変数の値を変数に保存
          GCP_PROJECT_ID_VAL="${{ secrets.GCP_PROJECT_ID }}"
          BQ_DATASET_VAL="${{ secrets.BQ_DATASET }}"
          GOOGLE_SPREADSHEET_ID_VAL="${{ secrets.GOOGLE_SPREADSHEET_ID }}"
          GOOGLE_SHEETS_API_KEY_VAL="${{ secrets.GOOGLE_SHEETS_API_KEY }}"
          GOOGLE_SHEET_NAME_VAL="${{ secrets.GOOGLE_SHEET_NAME }}"
          FRONTEND_URL_VAL="${{ secrets.FRONTEND_URL }}"
          
          # 必須環境変数の確認
          if [ -z "$GCP_PROJECT_ID_VAL" ]; then
            echo "❌ エラー: GCP_PROJECT_IDがGitHub Secretsに設定されていません"
            echo "   Environment secretsにGCP_PROJECT_IDを設定してください"
            echo "   https://github.com/${{ github.repository }}/settings/environments/Environment%20secrets"
            exit 1
          fi
          
          if [ -z "$BQ_DATASET_VAL" ]; then
            echo "❌ エラー: BQ_DATASETがGitHub Secretsに設定されていません"
            exit 1
          fi
          
          # 環境変数をカンマ区切りで構築
          ENV_VARS="GCP_PROJECT_ID=$GCP_PROJECT_ID_VAL,BQ_DATASET=$BQ_DATASET_VAL"
          
          if [ -n "$GOOGLE_SPREADSHEET_ID_VAL" ]; then
            ENV_VARS="$ENV_VARS,GOOGLE_SPREADSHEET_ID=$GOOGLE_SPREADSHEET_ID_VAL"
          fi
          
          if [ -n "$GOOGLE_SHEETS_API_KEY_VAL" ]; then
            ENV_VARS="$ENV_VARS,GOOGLE_SHEETS_API_KEY=$GOOGLE_SHEETS_API_KEY_VAL"
          fi
          
          if [ -n "$GOOGLE_SHEET_NAME_VAL" ]; then
            ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=$GOOGLE_SHEET_NAME_VAL"
          else
            ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=シート1"
          fi
          
          if [ -n "$FRONTEND_URL_VAL" ]; then
            ENV_VARS="$ENV_VARS,FRONTEND_URL=$FRONTEND_URL_VAL"
          else
            ENV_VARS="$ENV_VARS,FRONTEND_URL=http://localhost:5173"
          fi
          
          echo "📋 設定する環境変数:"
          echo "  GCP_PROJECT_ID: $GCP_PROJECT_ID_VAL"
          echo "  BQ_DATASET: $BQ_DATASET_VAL"
          echo "  GOOGLE_SPREADSHEET_ID: $([ -n "$GOOGLE_SPREADSHEET_ID_VAL" ] && echo 'SET' || echo 'NOT SET')"
          echo "  GOOGLE_SHEETS_API_KEY: $([ -n "$GOOGLE_SHEETS_API_KEY_VAL" ] && echo 'SET' || echo 'NOT SET')"
          echo "  GOOGLE_SHEET_NAME: ${GOOGLE_SHEET_NAME_VAL:-シート1}"
          echo "  FRONTEND_URL: ${FRONTEND_URL_VAL:-http://localhost:5173}"
          echo ""
          echo "📋 環境変数文字列（最初の100文字）:"
          echo "${ENV_VARS:0:100}..."
          echo ""
          
          # デプロイが完了するまで少し待機
          echo "⏳ デプロイ完了を待機中（10秒）..."
          sleep 10
          
          # 環境変数を設定（--update-env-varsを使用して既存の環境変数を保持）
          echo "🔧 gcloud run services update を実行中..."
          gcloud run services update ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --update-env-vars "$ENV_VARS" \
            --quiet
          
          echo "✅ 環境変数の設定が完了しました"
          
          # 設定を確認
          echo "🔍 設定された環境変数を確認中..."
          sleep 3
          
          VERIFIED_GCP_PROJECT_ID=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GCP_PROJECT_ID"].value)' 2>/dev/null || echo '')
          
          if [ -n "$VERIFIED_GCP_PROJECT_ID" ] && [ "$VERIFIED_GCP_PROJECT_ID" = "$GCP_PROJECT_ID_VAL" ]; then
            echo "✅ 確認: GCP_PROJECT_IDが正しく設定されました: $VERIFIED_GCP_PROJECT_ID"
          else
            echo "❌ エラー: GCP_PROJECT_IDの設定に失敗しました"
            echo "   期待値: $GCP_PROJECT_ID_VAL"
            echo "   実際の値: ${VERIFIED_GCP_PROJECT_ID:-NOT SET}"
            exit 1
          fi

      - name: Get backend URL
        id: get-backend-url
        run: |
          # deploy-cloudrunアクションからURLを取得
          BACKEND_URL="${{ steps.deploy.outputs.url }}"
          
          if [ -z "$BACKEND_URL" ]; then
            # フォールバック: gcloudコマンドで取得
            REGION_VALUE="${{ env.REGION }}"
            if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
              REGION_VALUE="asia-northeast1"
            fi
            
            BACKEND_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
              --region "$REGION_VALUE" \
              --project ${{ env.PROJECT_ID }} \
              --format 'value(status.url)')
          fi
          
          echo "backend_url=$BACKEND_URL" >> $GITHUB_OUTPUT
          echo "BACKEND_URL=$BACKEND_URL" >> $GITHUB_ENV
          echo "🚀 Backend deployed to: $BACKEND_URL"

      - name: Ensure Environment Variables Are Set
        if: always()
        run: |
          # リージョンの値を明示的に設定
          REGION_VALUE="${{ env.REGION }}"
          if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
            REGION_VALUE="asia-northeast1"
          fi
          
          echo "🔍 環境変数の設定を確認中..."
          
          # 現在の環境変数を確認
          CURRENT_GCP_PROJECT_ID=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GCP_PROJECT_ID"].value)' 2>/dev/null || echo '')
          
          # 環境変数が設定されていない場合、手動で設定
          if [ -z "$CURRENT_GCP_PROJECT_ID" ]; then
            echo "⚠️ GCP_PROJECT_IDが設定されていません。環境変数を設定します..."
            
            # 環境変数の値を変数に保存
            GCP_PROJECT_ID_VAL="${{ secrets.GCP_PROJECT_ID }}"
            BQ_DATASET_VAL="${{ secrets.BQ_DATASET }}"
            GOOGLE_SPREADSHEET_ID_VAL="${{ secrets.GOOGLE_SPREADSHEET_ID }}"
            GOOGLE_SHEETS_API_KEY_VAL="${{ secrets.GOOGLE_SHEETS_API_KEY }}"
            GOOGLE_SHEET_NAME_VAL="${{ secrets.GOOGLE_SHEET_NAME }}"
            FRONTEND_URL_VAL="${{ secrets.FRONTEND_URL }}"
            
            # 必須環境変数の確認
            if [ -z "$GCP_PROJECT_ID_VAL" ]; then
              echo "❌ エラー: GCP_PROJECT_IDがGitHub Secretsに設定されていません"
              exit 1
            fi
            
            if [ -z "$BQ_DATASET_VAL" ]; then
              echo "❌ エラー: BQ_DATASETがGitHub Secretsに設定されていません"
              exit 1
            fi
            
            # 環境変数を構築
            ENV_VARS="GCP_PROJECT_ID=$GCP_PROJECT_ID_VAL,BQ_DATASET=$BQ_DATASET_VAL"
            
            if [ -n "$GOOGLE_SPREADSHEET_ID_VAL" ]; then
              ENV_VARS="$ENV_VARS,GOOGLE_SPREADSHEET_ID=$GOOGLE_SPREADSHEET_ID_VAL"
            fi
            
            if [ -n "$GOOGLE_SHEETS_API_KEY_VAL" ]; then
              ENV_VARS="$ENV_VARS,GOOGLE_SHEETS_API_KEY=$GOOGLE_SHEETS_API_KEY_VAL"
            fi
            
            if [ -n "$GOOGLE_SHEET_NAME_VAL" ]; then
              ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=$GOOGLE_SHEET_NAME_VAL"
            else
              ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=シート1"
            fi
            
            if [ -n "$FRONTEND_URL_VAL" ]; then
              ENV_VARS="$ENV_VARS,FRONTEND_URL=$FRONTEND_URL_VAL"
            else
              ENV_VARS="$ENV_VARS,FRONTEND_URL=http://localhost:5173"
            fi
            
            echo "📋 環境変数を設定中..."
            echo "ENV_VARS: ${ENV_VARS:0:100}..."
            
            # 環境変数を設定
            gcloud run services update ${{ env.SERVICE_NAME }} \
              --region "$REGION_VALUE" \
              --project ${{ env.PROJECT_ID }} \
              --set-env-vars "$ENV_VARS" \
              --quiet
            
            echo "✅ 環境変数の設定が完了しました"
            
            # 再度確認
            sleep 3
            VERIFIED_GCP_PROJECT_ID=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
              --region "$REGION_VALUE" \
              --project ${{ env.PROJECT_ID }} \
              --format='value(spec.template.spec.containers[0].env[?name="GCP_PROJECT_ID"].value)' 2>/dev/null || echo '')
            
            if [ -n "$VERIFIED_GCP_PROJECT_ID" ]; then
              echo "✅ 確認: GCP_PROJECT_IDが正しく設定されました: $VERIFIED_GCP_PROJECT_ID"
            else
              echo "❌ 警告: 環境変数の設定に失敗した可能性があります"
            fi
          else
            echo "✅ GCP_PROJECT_IDは既に設定されています: $CURRENT_GCP_PROJECT_ID"
          fi

      - name: Verify Cloud Run Environment Variables
        if: always()
        run: |
          # リージョンの値を明示的に設定
          REGION_VALUE="${{ env.REGION }}"
          if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
            REGION_VALUE="asia-northeast1"
          fi
          
          echo "🔍 Cloud Runの環境変数を確認中..."
          echo ""
          
          # サービスがデプロイされるまで待機
          echo "サービスが完全にデプロイされるまで待機中（15秒）..."
          sleep 15
          
          # 環境変数を取得して表示
          echo "📋 設定されている環境変数:"
          echo "----------------------------------------"
          GCP_PROJECT_ID_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GCP_PROJECT_ID"].value)' 2>/dev/null || echo '')
          echo "GCP_PROJECT_ID: ${GCP_PROJECT_ID_VALUE:-NOT SET}"
          
          BQ_DATASET_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="BQ_DATASET"].value)' 2>/dev/null || echo '')
          echo "BQ_DATASET: ${BQ_DATASET_VALUE:-NOT SET}"
          
          GOOGLE_SPREADSHEET_ID_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GOOGLE_SPREADSHEET_ID"].value)' 2>/dev/null || echo '')
          echo "GOOGLE_SPREADSHEET_ID: ${GOOGLE_SPREADSHEET_ID_VALUE:-NOT SET}"
          
          GOOGLE_SHEETS_API_KEY_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GOOGLE_SHEETS_API_KEY"].value)' 2>/dev/null || echo '')
          echo "GOOGLE_SHEETS_API_KEY: $([ -n "$GOOGLE_SHEETS_API_KEY_VALUE" ] && echo 'SET' || echo 'NOT SET')"
          
          FRONTEND_URL_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="FRONTEND_URL"].value)' 2>/dev/null || echo '')
          echo "FRONTEND_URL: ${FRONTEND_URL_VALUE:-NOT SET}"
          echo "----------------------------------------"
          echo ""
          
          # 環境変数が設定されていない場合は自動的に設定
          if [ -z "$GCP_PROJECT_ID_VALUE" ] || [ "$GCP_PROJECT_ID_VALUE" = "NOT SET" ]; then
            echo "⚠️ GCP_PROJECT_IDが設定されていません。環境変数を自動設定します..."
            
            # 環境変数の値を変数に保存
            GCP_PROJECT_ID_VAL="${{ secrets.GCP_PROJECT_ID }}"
            BQ_DATASET_VAL="${{ secrets.BQ_DATASET }}"
            GOOGLE_SPREADSHEET_ID_VAL="${{ secrets.GOOGLE_SPREADSHEET_ID }}"
            GOOGLE_SHEETS_API_KEY_VAL="${{ secrets.GOOGLE_SHEETS_API_KEY }}"
            GOOGLE_SHEET_NAME_VAL="${{ secrets.GOOGLE_SHEET_NAME }}"
            FRONTEND_URL_VAL="${{ secrets.FRONTEND_URL }}"
            
            # 必須環境変数の確認
            if [ -z "$GCP_PROJECT_ID_VAL" ]; then
              echo "❌ エラー: GCP_PROJECT_IDがGitHub Secretsに設定されていません"
              echo "   Environment secretsにGCP_PROJECT_IDを設定してください"
              echo "   https://github.com/${{ github.repository }}/settings/environments/Environment%20secrets"
              exit 1
            fi
            
            if [ -z "$BQ_DATASET_VAL" ]; then
              echo "❌ エラー: BQ_DATASETがGitHub Secretsに設定されていません"
              exit 1
            fi
            
            # 環境変数を構築
            ENV_VARS="GCP_PROJECT_ID=$GCP_PROJECT_ID_VAL,BQ_DATASET=$BQ_DATASET_VAL"
            
            if [ -n "$GOOGLE_SPREADSHEET_ID_VAL" ]; then
              ENV_VARS="$ENV_VARS,GOOGLE_SPREADSHEET_ID=$GOOGLE_SPREADSHEET_ID_VAL"
            fi
            
            if [ -n "$GOOGLE_SHEETS_API_KEY_VAL" ]; then
              ENV_VARS="$ENV_VARS,GOOGLE_SHEETS_API_KEY=$GOOGLE_SHEETS_API_KEY_VAL"
            fi
            
            if [ -n "$GOOGLE_SHEET_NAME_VAL" ]; then
              ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=$GOOGLE_SHEET_NAME_VAL"
            else
              ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=シート1"
            fi
            
            if [ -n "$FRONTEND_URL_VAL" ]; then
              ENV_VARS="$ENV_VARS,FRONTEND_URL=$FRONTEND_URL_VAL"
            else
              ENV_VARS="$ENV_VARS,FRONTEND_URL=http://localhost:5173"
            fi
            
            echo "📋 環境変数を設定中..."
            echo "ENV_VARS: ${ENV_VARS:0:100}..."
            echo ""
            
            # 環境変数を設定
            gcloud run services update ${{ env.SERVICE_NAME }} \
              --region "$REGION_VALUE" \
              --project ${{ env.PROJECT_ID }} \
              --set-env-vars "$ENV_VARS" \
              --quiet
            
            echo "✅ 環境変数の設定が完了しました"
            
            # 再度確認
            echo "🔍 環境変数の設定を再確認中..."
            sleep 5
            VERIFIED_GCP_PROJECT_ID=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
              --region "$REGION_VALUE" \
              --project ${{ env.PROJECT_ID }} \
              --format='value(spec.template.spec.containers[0].env[?name="GCP_PROJECT_ID"].value)' 2>/dev/null || echo '')
            
            if [ -n "$VERIFIED_GCP_PROJECT_ID" ] && [ "$VERIFIED_GCP_PROJECT_ID" != "NOT SET" ]; then
              echo "✅ 確認: GCP_PROJECT_IDが正しく設定されました: $VERIFIED_GCP_PROJECT_ID"
            else
              echo "❌ エラー: 環境変数の設定に失敗しました"
              echo ""
              echo "📝 手動で環境変数を設定してください:"
              echo "   gcloud run services update ${{ env.SERVICE_NAME }} \\"
              echo "     --set-env-vars GCP_PROJECT_ID=\"$GCP_PROJECT_ID_VAL\",BQ_DATASET=\"$BQ_DATASET_VAL\" \\"
              echo "     --region $REGION_VALUE \\"
              echo "     --project ${{ env.PROJECT_ID }}"
              exit 1
            fi
          else
            echo "✅ 環境変数の確認が完了しました"
          fi

      - name: Update GitHub Secret (Backend URL)
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "💡 Backend URL: $BACKEND_URL"
          echo "💡 このURLをGitHub Secretsの BACKEND_URL に設定してください"
          echo "💡 または、フロントエンドデプロイ時に環境変数として使用します"

      - name: Deployment Summary
        run: |
          echo "## ✅ Backend Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Service**: ${{ env.SERVICE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Region**: ${{ env.REGION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL**: [$BACKEND_URL]($BACKEND_URL)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📝 Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "1. このURLをフロントエンドの環境変数 `VITE_API_BASE_URL` に設定" >> $GITHUB_STEP_SUMMARY
          echo "2. フロントエンドをデプロイ" >> $GITHUB_STEP_SUMMARY

