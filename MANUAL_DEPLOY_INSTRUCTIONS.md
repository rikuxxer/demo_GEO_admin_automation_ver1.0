# 手動デプロイ手順

## 🔍 問題

最後のコミットは`.github/workflows/deploy-backend.yml`の変更のみで、`backend/**`パスに変更がないため、自動デプロイがトリガーされていません。

## ✅ 解決方法

### 方法1: GitHub Actionsで手動実行（推奨）

1. GitHubリポジトリを開く
2. **Actions**タブをクリック
3. 左側のメニューから「**Deploy Backend to Cloud Run**」を選択
4. 右上の「**Run workflow**」ボタンをクリック
5. **environment**を選択（デフォルト: `production`）
6. 「**Run workflow**」ボタンをクリック

### 方法2: ダミーファイルを作成してpush

`backend/**`パスに変更を加えることで、自動デプロイをトリガーします：

```bash
# ダミーファイルを作成（または既存ファイルを少し変更）
echo "# Trigger deployment" >> backend/.deploy-trigger

git add backend/.deploy-trigger
git commit -m "trigger: バックエンドデプロイをトリガー"
git push
```

### 方法3: 既存のbackendファイルを少し変更

```bash
# package.jsonにコメントを追加（実質的な変更なし）
# または、既存のファイルを少し変更
```

## 📋 デプロイが完了したら

1. GitHub Actionsのログでデプロイが成功したことを確認
2. バックエンドURLを確認
3. `test-spreadsheet-export.html`でテストを実行

