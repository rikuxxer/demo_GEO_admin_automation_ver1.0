name: Deploy Backend to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID || vars.GCP_PROJECT_ID }}
  REGION: asia-northeast1
  SERVICE_NAME: universegeo-backend

jobs:
  deploy-backend:
    name: Deploy Backend to Cloud Run
    runs-on: ubuntu-latest
    
    environment:
      name: Environment secrets
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug Secrets
        run: |
          echo "🔍 デバッグ情報:"
          echo "GitHub Event: ${{ github.event_name }}"
          echo "GitHub Ref: ${{ github.ref }}"
          echo "GitHub Repository: ${{ github.repository }}"
          echo ""
          echo "📋 Secrets確認（値は表示しません）:"
          echo "GCP_SA_KEY exists: $([ -n "${{ secrets.GCP_SA_KEY }}" ] && echo 'YES' || echo 'NO')"
          echo "GCP_SA_KEY length: $(echo -n "${{ secrets.GCP_SA_KEY }}" | wc -c)"
          echo "GCP_PROJECT_ID exists: $([ -n "${{ secrets.GCP_PROJECT_ID }}" ] && echo 'YES' || echo 'NO')"
          echo "GCP_PROJECT_ID value: ${{ secrets.GCP_PROJECT_ID }}"
          echo ""
          echo "📋 環境変数:"
          echo "PROJECT_ID: ${{ env.PROJECT_ID }}"
          echo "REGION: ${{ env.REGION }}"
          echo "SERVICE_NAME: ${{ env.SERVICE_NAME }}"
          echo ""
          echo "📝 確認方法:"
          echo "1. https://github.com/${{ github.repository }}/settings/secrets/actions を開く"
          echo "2. Repository secrets タブで以下を確認:"
          echo "   - GCP_SA_KEY が存在するか"
          echo "   - GCP_PROJECT_ID が存在するか"
          echo "   - Secret名が正確に一致しているか（大文字・小文字）"

      - name: Verify Secrets
        run: |
          echo "🔍 Secret確認中..."
          echo ""
          
          # GCP_SA_KEY の確認（Repository secrets または Environment secrets）
          GCP_SA_KEY_VALUE='${{ secrets.GCP_SA_KEY }}'
          GCP_SA_KEY_LENGTH=$(echo -n "$GCP_SA_KEY_VALUE" | wc -c)
          
          echo "GCP_SA_KEY length: $GCP_SA_KEY_LENGTH"
          
          if [ -z "$GCP_SA_KEY_VALUE" ] || [ "$GCP_SA_KEY_LENGTH" -eq 0 ] || [ "$GCP_SA_KEY_VALUE" = "***" ]; then
            echo "❌ エラー: GCP_SA_KEY が設定されていません"
            echo ""
            echo "📝 設定方法（Environment secrets）:"
            echo ""
            echo "このワークフローは 'environment: Environment secrets' を設定しているため、"
            echo "Environment secrets を参照します。"
            echo ""
            echo "設定手順:"
            echo "1. https://github.com/${{ github.repository }}/settings/environments/Environment%20secrets を開く"
            echo "2. Environment secrets セクションで Add secret をクリック"
            echo "3. Name: GCP_SA_KEY（大文字・小文字を正確に）"
            echo "   ⚠️ 重要: Secret名は 'GCP_SA_KEY' と完全に一致する必要があります"
            echo "   ❌ 間違い: gcp_sa_key, GCP-SA-KEY, GCP_SA_KEY_ など"
            echo "4. Secret: サービスアカウントキー（JSON）の内容全体を貼り付け"
            echo "   - JSON全体（{ から } まで）をそのまま貼り付け"
            echo "   - 改行も含めてそのまま貼り付け"
            echo "5. Add secret をクリック"
            echo ""
            echo "⚠️ 確認事項:"
            echo "- Environment secrets に 'GCP_SA_KEY' が存在するか確認"
            echo "- Secret名が正確に 'GCP_SA_KEY' であるか確認（大文字・小文字）"
            echo "- JSONファイルの内容全体をコピー&ペーストしているか確認"
            exit 1
          fi
          
          echo "✅ GCP_SA_KEY が設定されています（長さ: $GCP_SA_KEY_LENGTH 文字）"
          
          # JSON形式の確認
          if echo "$GCP_SA_KEY_VALUE" | grep -q '"type": "service_account"'; then
            echo "✅ JSON形式が正しいです"
          else
            echo "⚠️ 警告: JSON形式が正しくない可能性があります"
          fi
          
          # GCP_PROJECT_ID の確認
          GCP_PROJECT_ID_VALUE="${{ secrets.GCP_PROJECT_ID }}"
          if [ -z "$GCP_PROJECT_ID_VALUE" ]; then
            echo "❌ エラー: GCP_PROJECT_ID が設定されていません"
            echo ""
            echo "📝 設定方法:"
            echo "1. Settings > Secrets and variables > Actions > Repository secrets"
            echo "2. Name: GCP_PROJECT_ID"
            echo "3. Secret: univere-geo-demo"
            exit 1
          fi
          echo "✅ GCP_PROJECT_ID: $GCP_PROJECT_ID_VALUE"
          
          # その他のSecretsの確認
          echo ""
          echo "📋 その他のSecrets確認:"
          echo "BQ_DATASET: $([ -z "${{ secrets.BQ_DATASET }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"
          echo "GOOGLE_SPREADSHEET_ID: $([ -z "${{ secrets.GOOGLE_SPREADSHEET_ID }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"
          echo "GOOGLE_SHEETS_API_KEY: $([ -z "${{ secrets.GOOGLE_SHEETS_API_KEY }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"
          echo "BACKEND_SERVICE_ACCOUNT: $([ -z "${{ secrets.BACKEND_SERVICE_ACCOUNT }}" ] && echo "❌ 未設定" || echo "✅ 設定済み")"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Verify Backend Files
        working-directory: ./backend
        run: |
          echo "🔍 バックエンドファイルの確認:"
          echo ""
          echo "📁 ディレクトリ構造:"
          ls -la
          echo ""
          echo "📄 重要なファイルの確認:"
          echo "  Dockerfile: $([ -f Dockerfile ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo "  package.json: $([ -f package.json ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo "  tsconfig.json: $([ -f tsconfig.json ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo "  src/index.ts: $([ -f src/index.ts ] && echo '✅ 存在' || echo '❌ 存在しない')"
          echo ""
          if [ -f Dockerfile ]; then
            echo "📋 Dockerfileの内容:"
            head -20 Dockerfile
          fi

      - name: Deploy to Cloud Run
        working-directory: ./backend
        run: |
          echo "🚀 Deploying backend to Cloud Run..."
          echo ""
          echo "📋 デプロイ設定:"
          echo "  Service: ${{ env.SERVICE_NAME }}"
          echo "  Region: ${{ env.REGION }}"
          echo "  Project: ${{ env.PROJECT_ID }}"
          echo "  Service Account: (デフォルト - 権限エラーを回避するため)"
          echo ""
          echo "💡 注意: サービスアカウントの権限エラーを回避するため、"
          echo "   デフォルトのサービスアカウントを使用します。"
          echo "   必要に応じて、後でCloud Runサービスの設定から変更できます。"
          echo ""
          
          # リージョンの値を明示的に設定（デバッグ用）
          REGION_VALUE="${{ env.REGION }}"
          echo "🔍 デバッグ: REGION_VALUE = '$REGION_VALUE'"
          if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
            echo "⚠️ 警告: リージョン値が予期しない値です。'asia-northeast1' に設定します。"
            REGION_VALUE="asia-northeast1"
          fi
          echo "✅ 使用するリージョン: $REGION_VALUE"
          echo ""
          
          # 環境変数の値を確認（デバッグ用）
          echo "🔍 環境変数の確認（デバッグ）:"
          echo "  GCP_PROJECT_ID: $([ -n "${{ secrets.GCP_PROJECT_ID }}" ] && echo 'SET (length: '$(echo -n "${{ secrets.GCP_PROJECT_ID }}" | wc -c)')' || echo 'NOT SET')"
          echo "  BQ_DATASET: $([ -n "${{ secrets.BQ_DATASET }}" ] && echo 'SET' || echo 'NOT SET')"
          echo ""
          
          # 環境変数が設定されていない場合はエラー
          if [ -z "${{ secrets.GCP_PROJECT_ID }}" ]; then
            echo "❌ エラー: GCP_PROJECT_IDがGitHub Secretsに設定されていません"
            echo "   Environment secretsにGCP_PROJECT_IDを設定してください"
            echo "   https://github.com/${{ github.repository }}/settings/environments/Environment%20secrets"
            exit 1
          fi
          
          # デフォルトのサービスアカウントでデプロイ（権限エラーを回避）
          # 環境変数を1つの--set-env-varsオプションで設定（カンマ区切り）
          # 注意: 値にカンマや特殊文字が含まれる場合は、個別に--update-env-varsを使用する必要がある
          
          echo "📋 設定する環境変数:"
          echo "  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}"
          echo "  BQ_DATASET: ${{ secrets.BQ_DATASET }}"
          echo "  GOOGLE_SPREADSHEET_ID: ${{ secrets.GOOGLE_SPREADSHEET_ID }}"
          echo "  GOOGLE_SHEETS_API_KEY: $([ -n "${{ secrets.GOOGLE_SHEETS_API_KEY }}" ] && echo 'SET' || echo 'NOT SET')"
          echo "  GOOGLE_SHEET_NAME: ${{ secrets.GOOGLE_SHEET_NAME || 'シート1' }}"
          echo "  FRONTEND_URL: ${{ secrets.FRONTEND_URL || 'http://localhost:5173' }}"
          echo ""
          
          # 環境変数をカンマ区切りで構築
          ENV_VARS="GCP_PROJECT_ID=${{ secrets.GCP_PROJECT_ID }},BQ_DATASET=${{ secrets.BQ_DATASET }}"
          
          # オプショナルな環境変数を追加
          if [ -n "${{ secrets.GOOGLE_SPREADSHEET_ID }}" ]; then
            ENV_VARS="$ENV_VARS,GOOGLE_SPREADSHEET_ID=${{ secrets.GOOGLE_SPREADSHEET_ID }}"
          fi
          
          if [ -n "${{ secrets.GOOGLE_SHEETS_API_KEY }}" ]; then
            ENV_VARS="$ENV_VARS,GOOGLE_SHEETS_API_KEY=${{ secrets.GOOGLE_SHEETS_API_KEY }}"
          fi
          
          if [ -n "${{ secrets.GOOGLE_SHEET_NAME }}" ]; then
            ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=${{ secrets.GOOGLE_SHEET_NAME }}"
          else
            ENV_VARS="$ENV_VARS,GOOGLE_SHEET_NAME=シート1"
          fi
          
          if [ -n "${{ secrets.FRONTEND_URL }}" ]; then
            ENV_VARS="$ENV_VARS,FRONTEND_URL=${{ secrets.FRONTEND_URL }}"
          else
            ENV_VARS="$ENV_VARS,FRONTEND_URL=http://localhost:5173"
          fi
          
          echo "📋 構築された環境変数文字列:"
          echo "$ENV_VARS" | tr ',' '\n' | sed 's/^/  /'
          echo ""
          
          set +e
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --source . \
            --region "$REGION_VALUE" \
            --allow-unauthenticated \
            --memory 512Mi \
            --cpu 1 \
            --max-instances 10 \
            --set-env-vars "$ENV_VARS" \
            --project ${{ env.PROJECT_ID }}
          DEPLOY_EXIT_CODE=$?
          set -e
          
          if [ $DEPLOY_EXIT_CODE -ne 0 ]; then
            echo ""
            echo "❌ デプロイが失敗しました"
            echo ""
            echo "📋 ビルドログを取得中..."
            echo ""
            
            # プロジェクトIDを変数に保存（マスクされないように）
            PROJECT_ID="${{ env.PROJECT_ID }}"
            echo "プロジェクトID: $PROJECT_ID"
            echo ""
            
            # 少し待ってからビルドログを取得（ビルドが完了するまで待つ）
            echo "ビルド完了を待機中（25秒）..."
            sleep 25
            
            # 最新のビルドIDを取得（複数の方法を試す）
            echo "ビルドIDを取得中..."
            LATEST_BUILD=$(gcloud builds list --limit=1 --sort-by=~createTime --format='value(id)' --project "$PROJECT_ID" 2>&1 | head -1)
            
            # 警告メッセージを除外
            if echo "$LATEST_BUILD" | grep -q "WARNING"; then
              LATEST_BUILD=$(echo "$LATEST_BUILD" | grep -v "WARNING" | head -1)
            fi
            
            if [ -z "$LATEST_BUILD" ] || [ "$LATEST_BUILD" = "" ] || [ "${LATEST_BUILD:0:1}" = "W" ]; then
              echo "別の方法でビルドIDを取得中..."
              LATEST_BUILD=$(gcloud builds list --limit=1 --format='value(id)' --project "$PROJECT_ID" 2>&1 | grep -v "WARNING" | head -1)
            fi
            
            if [ -n "$LATEST_BUILD" ] && [ "$LATEST_BUILD" != "" ] && [ "${LATEST_BUILD:0:1}" != "W" ] && [ "${#LATEST_BUILD}" -gt 10 ]; then
              echo "✅ 最新のビルドID: $LATEST_BUILD"
              echo ""
              echo "📋 ビルドログ（最後の500行）:"
              echo "=========================================="
              gcloud builds log "$LATEST_BUILD" --project "$PROJECT_ID" 2>&1 | tail -500
              echo "=========================================="
              echo ""
              echo "📋 エラー部分のみ抽出:"
              echo "----------------------------------------"
              gcloud builds log "$LATEST_BUILD" --project "$PROJECT_ID" 2>&1 | grep -i "error\|failed\|fail\|ERROR\|FAILED" | tail -50
              echo "----------------------------------------"
              echo ""
              echo "📋 ビルドの詳細情報:"
              gcloud builds describe "$LATEST_BUILD" --project "$PROJECT_ID" --format='yaml(status,logUrl,steps)' 2>&1 | head -100
            else
            echo "⚠️ ビルドIDを取得できませんでした"
            echo "取得結果: $LATEST_BUILD"
            echo ""
            echo "最近のビルド一覧（デバッグ情報）:"
            echo "----------------------------------------"
            gcloud builds list --limit=5 --project "$PROJECT_ID" 2>&1
            echo "----------------------------------------"
            echo ""
            echo "💡 代替方法: サービス名からビルドを検索..."
            SERVICE_NAME="${{ env.SERVICE_NAME }}"
            echo "サービス名: $SERVICE_NAME"
            gcloud builds list --filter="substitutions.SERVICE_NAME=$SERVICE_NAME" --limit=1 --project "$PROJECT_ID" 2>&1
            fi
            
            echo ""
            echo "📋 ビルドログを確認する方法:"
            echo ""
            echo "方法1: Google Cloud Console（推奨）"
            echo "   1. https://console.cloud.google.com/cloud-build/builds を開く"
            echo "   2. プロジェクトセレクタでプロジェクトを選択"
            echo "      （プロジェクトID: $PROJECT_ID）"
            echo "   3. 最新のビルドをクリック"
            echo "   4. ビルドログを確認"
            echo ""
            echo "   直接リンク（プロジェクトIDを手動で置き換えてください）:"
            echo "   https://console.cloud.google.com/cloud-build/builds?project=$PROJECT_ID"
            echo ""
            echo "方法2: gcloud CLI（ローカルで実行）"
            echo "   # 最新のビルドIDを取得"
            echo "   gcloud builds list --limit=1 --project $PROJECT_ID"
            echo ""
            echo "   # ビルドログを表示（BUILD_IDを実際のIDに置き換え）"
            echo "   gcloud builds log [BUILD_ID] --project $PROJECT_ID"
            echo ""
            echo "💡 ヒント: ビルドが失敗している場合、以下の点を確認してください:"
            echo "   - Dockerfileが正しく配置されているか"
            echo "   - package.jsonとtsconfig.jsonが存在するか"
            echo "   - 必要なファイルがすべてコピーされているか"
            echo "   - TypeScriptのコンパイルエラーがないか"
            echo "   - npm installが正常に完了しているか"
            echo ""
            echo "🔍 プロジェクトIDの確認方法:"
            echo "   このワークフローで使用しているプロジェクトIDは: $PROJECT_ID"
            echo "   GitHub Secretsで 'GCP_PROJECT_ID' として設定されている値です"
            echo ""
            exit $DEPLOY_EXIT_CODE
          fi

      - name: Get backend URL
        id: get-backend-url
        run: |
          # リージョンの値を明示的に設定
          REGION_VALUE="${{ env.REGION }}"
          if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
            REGION_VALUE="asia-northeast1"
          fi
          
          BACKEND_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --format 'value(status.url)')
          
          echo "backend_url=$BACKEND_URL" >> $GITHUB_OUTPUT
          echo "BACKEND_URL=$BACKEND_URL" >> $GITHUB_ENV
          echo "🚀 Backend deployed to: $BACKEND_URL"

      - name: Verify Cloud Run Environment Variables
        run: |
          # リージョンの値を明示的に設定
          REGION_VALUE="${{ env.REGION }}"
          if [ -z "$REGION_VALUE" ] || [ "$REGION_VALUE" != "asia-northeast1" ]; then
            REGION_VALUE="asia-northeast1"
          fi
          
          echo "🔍 Cloud Runの環境変数を確認中..."
          echo ""
          
          # サービスがデプロイされるまで待機
          echo "サービスが完全にデプロイされるまで待機中（15秒）..."
          sleep 15
          
          # 環境変数を取得して表示
          echo "📋 設定されている環境変数:"
          echo "----------------------------------------"
          GCP_PROJECT_ID_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GCP_PROJECT_ID"].value)' 2>/dev/null || echo 'NOT SET')
          echo "GCP_PROJECT_ID: $GCP_PROJECT_ID_VALUE"
          
          BQ_DATASET_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="BQ_DATASET"].value)' 2>/dev/null || echo 'NOT SET')
          echo "BQ_DATASET: $BQ_DATASET_VALUE"
          
          GOOGLE_SPREADSHEET_ID_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GOOGLE_SPREADSHEET_ID"].value)' 2>/dev/null || echo 'NOT SET')
          echo "GOOGLE_SPREADSHEET_ID: $GOOGLE_SPREADSHEET_ID_VALUE"
          
          GOOGLE_SHEETS_API_KEY_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="GOOGLE_SHEETS_API_KEY"].value)' 2>/dev/null || echo 'NOT SET')
          echo "GOOGLE_SHEETS_API_KEY: $([ -n "$GOOGLE_SHEETS_API_KEY_VALUE" ] && echo 'SET' || echo 'NOT SET')"
          
          FRONTEND_URL_VALUE=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region "$REGION_VALUE" \
            --project ${{ env.PROJECT_ID }} \
            --format='value(spec.template.spec.containers[0].env[?name="FRONTEND_URL"].value)' 2>/dev/null || echo 'NOT SET')
          echo "FRONTEND_URL: $FRONTEND_URL_VALUE"
          echo "----------------------------------------"
          echo ""
          
          # 環境変数が設定されていない場合は警告
          if [ "$GCP_PROJECT_ID_VALUE" = "NOT SET" ] || [ -z "$GCP_PROJECT_ID_VALUE" ]; then
            echo "❌ エラー: GCP_PROJECT_IDがCloud Runに設定されていません！"
            echo ""
            echo "📝 解決方法:"
            echo "1. GitHub Secrets（Environment secrets）にGCP_PROJECT_IDが設定されているか確認"
            echo "   https://github.com/${{ github.repository }}/settings/environments/Environment%20secrets"
            echo "2. 環境変数を手動で設定する場合:"
            echo "   gcloud run services update ${{ env.SERVICE_NAME }} \\"
            echo "     --set-env-vars GCP_PROJECT_ID=\"${{ secrets.GCP_PROJECT_ID }}\" \\"
            echo "     --region $REGION_VALUE \\"
            echo "     --project ${{ env.PROJECT_ID }}"
            exit 1
          fi
          
          echo "✅ 環境変数の確認が完了しました"

      - name: Update GitHub Secret (Backend URL)
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "💡 Backend URL: $BACKEND_URL"
          echo "💡 このURLをGitHub Secretsの BACKEND_URL に設定してください"
          echo "💡 または、フロントエンドデプロイ時に環境変数として使用します"

      - name: Deployment Summary
        run: |
          echo "## ✅ Backend Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Service**: ${{ env.SERVICE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Region**: ${{ env.REGION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL**: [$BACKEND_URL]($BACKEND_URL)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📝 Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "1. このURLをフロントエンドの環境変数 `VITE_API_BASE_URL` に設定" >> $GITHUB_STEP_SUMMARY
          echo "2. フロントエンドをデプロイ" >> $GITHUB_STEP_SUMMARY

