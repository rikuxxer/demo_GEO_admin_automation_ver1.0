# Cloud Shellでのファイル設定方法

## 📋 方法1: ファイルを直接作成（推奨・最も簡単）

Cloud Shellで以下のコマンドを実行して、ファイルの内容を直接作成します：

```bash
# update_all_schemas_complete.shファイルを作成
cat > update_all_schemas_complete.sh << 'SCRIPT_EOF'
```

その後、`update_all_schemas_complete.sh`の**全内容をコピー&ペースト**して、最後に以下を追加：

```bash
SCRIPT_EOF
```

これでファイルが作成されます。

---

## 📤 方法2: Cloud Shellのアップロード機能を使用

### ステップ1: Cloud Shellでファイルアップロードボタンをクリック

1. Cloud Shellの上部にある**「⋮」（3つの点）メニュー**をクリック
2. **「ファイルをアップロード」**を選択
3. ファイル選択ダイアログで`update_all_schemas_complete.sh`を選択

### ステップ2: ファイルがアップロードされたことを確認

```bash
# ファイルが存在するか確認
ls -la update_all_schemas_complete.sh

# ファイルの内容を確認（最初の20行）
head -20 update_all_schemas_complete.sh
```

---

## 📋 方法3: ローカルからCloud Shellにコピー（gcloudコマンド使用）

ローカルマシンからCloud Shellにファイルをコピーする場合：

```bash
# ローカルマシンで実行（Windows PowerShellの場合）
gcloud cloud-shell scp local:update_all_schemas_complete.sh cloudshell:~/update_all_schemas_complete.sh

# または、Cloud Shellで実行（ファイルが既にCloud Shellのホームディレクトリにある場合）
# 何もしなくてOK
```

---

## 🚀 最も簡単な方法（推奨）

Cloud Shellで以下のコマンドを**1行ずつ**実行してください：

### ステップ1: ファイルを作成

```bash
cat > update_all_schemas_complete.sh << 'SCRIPT_EOF'
```

### ステップ2: ファイルの内容をコピー&ペースト

`update_all_schemas_complete.sh`の**全内容**をコピーして、Cloud Shellに貼り付けます。

### ステップ3: ファイルの終了を指定

貼り付けた後、最後に以下を入力：

```bash
SCRIPT_EOF
```

### ステップ4: 実行権限を付与

```bash
chmod +x update_all_schemas_complete.sh
```

### ステップ5: 実行

```bash
./update_all_schemas_complete.sh
```

---

## ✅ ファイルが正しく作成されたか確認

```bash
# ファイルが存在するか確認
ls -la update_all_schemas_complete.sh

# ファイルの行数を確認（271行であることを確認）
wc -l update_all_schemas_complete.sh

# ファイルの最初の数行を確認
head -10 update_all_schemas_complete.sh

# ファイルの最後の数行を確認
tail -10 update_all_schemas_complete.sh
```

---

## 🔧 トラブルシューティング

### エラー: "command not found: cat"

Cloud Shellでは通常`cat`コマンドは利用可能です。もしエラーが出る場合は：

```bash
# エディタを使用（nano）
nano update_all_schemas_complete.sh
```

その後、ファイルの内容を貼り付けて、`Ctrl+O`で保存、`Ctrl+X`で終了します。

### エラー: "Permission denied"

実行権限が付与されていない場合：

```bash
chmod +x update_all_schemas_complete.sh
```

### ファイルの内容が正しくない場合

ファイルを削除して再作成：

```bash
# ファイルを削除
rm update_all_schemas_complete.sh

# 再度作成（上記の方法1を参照）
```

---

## 📝 注意事項

- Cloud Shellのホームディレクトリ（`~`）にファイルが作成されます
- ファイルはCloud Shellセッション終了後も保持されます
- 複数のCloud Shellセッション間でファイルは共有されます

